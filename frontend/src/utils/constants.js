import pagesData from '../data/pages.json';

export const VENDOR_CATEGORIES = pagesData.vendorCategories;
export const MOCK_VENDORS = pagesData.mockVendors;

export const USER_ROLES = {
    USER: 'user',
    VENDOR: 'vendor',
    ADMIN: 'admin'
};
